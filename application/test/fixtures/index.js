const userProvider = require('./user');
const currenciesProvider = require('./currency');
const walletProvider = require('./wallet');

module.exports = {
    userProvider,
    currenciesProvider,
    walletProvider
};